# environment.yml
name: miphei
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pyvips=3.1.1
  - pyarrow=20.0.0
  - pip
  - pip:
    - -r requirements.txt
    - -e ./slidevips-python
 